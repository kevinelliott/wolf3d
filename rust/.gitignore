/target
/dist
# Cargo.lock is intentionally committed: this crate is a binary/application,
# so a pinned lockfile gives reproducible CI and release builds.
*.bk
*.swp
.DS_Store

# Original game data (user-supplied; shareware WL1 is freely distributable)
gamedata/*.WL1
gamedata/*.WL6
gamedata/*.SOD
gamedata/*.SD*
/target
*.ppm
